{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('user_home') }}
{% endblock %}

{% block main %}

{% import 'breadcrumbs.html.twig' as breadcrumbs %}
{{ breadcrumbs.render([
    {'url': url('app_home'), 'text': 'Accueil'},
    {'url': url('accueil_hote'), 'text': 'Profil hote'},
]) }}


    <div id="profil-title">
        <h1>Mon profil</h1> 
        <div>
            <h2>{{ utilisateur.nom }} {{ utilisateur.prenom }} :</h2>
            <h3>{{ utilisateur.email }}</h3>
        </div>
    </div>
    <div id="card-container">
        {# <a href="{{ path('informations_personnelles', {'id': app.user.id } ) }}" class="data-card col-md-3">
            <h5>Informations personnelles</h5>
            <p>Verifier et modifier mes informations personnelles.</p>
        </a>
        <a href="{{ path('reservations') }}" class="data-card col-md-3">
            <h5>Mes réservations</h5>
            <p>Vérifier et modifier mes réservations.</p>
        </a> #}
        <a href="{{ path('hote_habitats') }}" class="data-card col-md-8">
            <h5>Mes habitats</h5>
            <p>Vérifier et modifier mes habitats.</p>
        </a> 
    </div>

    <div class="fond_vert_radius"></div>
{% endblock %}
